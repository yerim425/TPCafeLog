<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TPCafeLog" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="android:windowBackground">@color/white</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="colorOnBackground">@color/gray</item>
        <item name="android:colorPrimary">@color/brown</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/mocha</item>
        <item name="colorOnSecondary">@color/white</item>

        <item name="popupMenuStyle">@style/MyPopupMenu</item><!-- 메뉴 팝업 bg color-->
    </style>

    <style name="Theme.TPCafeLog" parent="Base.Theme.TPCafeLog" />

    <!--??-->
    <style name="MyPopupMenu" parent="Widget.AppCompat.PopupMenu">
        <item name="android:popupBackground">@color/white</item>
    </style>

    <style name="DialogToolbarTitleStyle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">16sp</item>
    </style>
</resources>